RSpec.describe Rubocop::Disallow::Safe::Navigation do
  it "has a version number" do
    expect(Rubocop::Disallow::Safe::Navigation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
